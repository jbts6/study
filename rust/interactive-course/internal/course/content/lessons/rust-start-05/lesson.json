{
  "title": "借用、切片与生命周期",
  "goal": "让返回引用的生命周期跟随输入，而不是引用局部数据。",
  "explanation": "切片函数借用调用者的数据，不负责释放它。生命周期标注描述输入和输出引用的关系，不能让局部变量的引用逃出作用域。",
  "exerciseGoal": "找到最长单词，并用可变切片标记任务完成。",
  "hints": [
    "longest_word 的返回引用必须来自 text，函数签名可以显式写出生命周期。",
    "get_mut 返回 Option<&mut bool>，索引越界时返回 false。"
  ],
  "tests": [
    {"id": "longest_word", "label": "返回最长单词切片"},
    {"id": "mark_done", "label": "安全修改可变切片"}
  ]
}
