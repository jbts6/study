# AGENTS.md

## 项目性质

奥术工业幻想 Python RPG：单人本机学习的回合制战术 RPG。玩家在本地运行自己的 Python 代码来施法和制定战术。非公网部署，非多用户，不接收外部不可信输入。

## 安全模型：信任本地代码

本项目采用**信任本地代码模型**，不是安全沙箱，不对抗恶意代码。玩家在本机运行自己的代码。

### 以下 API 是本地代码执行器的合理使用，审查时不应标记为安全风险

| API | 用途 | 为什么不是风险 |
|---|---|---|
| `exec()` / `compile()` | execute.py 执行玩家 Python 代码 | 本地代码执行，不是远程代码注入 |
| `child_process.spawn` | Node 启动本地 Python 子进程 | 只监听 localhost，不暴露公网 |
| `SIGINT` / `SIGKILL` | 中断和超时重建子进程 | 管理本地进程生命周期 |
| `sys.modules` / `sys.meta_path` 操控 | Python 模块隔离 | 不是猴子补丁攻击 |
| `__import__` 替换 | 模块白名单 | 防误操作（如 import socket 联网），不防恶意攻击 |
| `shutil.rmtree` | 清理临时工作目录 | 只删 tempfile 创建的目录，不删系统文件 |

### 术语约定

- 用"工作目录隔离"/"进程隔离"，**不用"沙箱"**——沙箱暗示对抗恶意代码的隔离边界，本项目明确不做这个
- 用"本地代码执行器"，**不用"远程代码执行"**——代码在玩家本机运行，不经过网络

### 头脑风暴前提

讨论本地 Runner 设计前先确认：本项目采用信任本地代码模型，玩家运行自己的代码，不涉及对抗恶意代码或远程代码执行。

### 真正的安全问题（需要修复）

- 监听 `0.0.0.0` 或公网地址的网络服务
- 接收外部不可信输入并执行代码
- 将玩家代码发送到远程服务器
- 绕过模块白名单允许 `import socket` / `ssl` / `http` / `subprocess`
