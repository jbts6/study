# AGENTS.md

## 项目性质

奥术工业幻想 Python RPG：单人本机学习的回合制战术 RPG。玩家在本地运行自己的 Python 代码来施法和制定战术。非公网部署，非多用户，不接收外部不可信输入。

## 最高工程前提：个人游戏、仅本地运行

- 一切功能、抽象、防护和测试都必须直接服务本地单人六关主流程，或对应已经复现的真实玩家问题；否则默认不做。
- 不按公网产品、多用户服务、恶意代码执行平台或长期兼容产品的标准设计本项目。
- 优先保证玩家能编辑 Python、看懂错误、执行回合、推进关卡和恢复当前存档；不得用防护措施限制正常 Python 学习体验。
- 只处理常见本地故障：语法错误、运行时错误、超时、中断、Python 不可用和存档 JSON 损坏。
- 不为极端进程竞态、重复关闭、信号排列、Proxy/访问器、对象篡改、跨版本迁移或未来扩展增加状态机、协议字段和测试矩阵。
- 测试覆盖正常路径和一个关键失败路径。先简化实现，再删除只验证旧实现细节的测试；不得只为提高测试数量增加用例。

## VS Code 扩展交付

- 修改 `rpg/` 中会进入 VS Code 扩展或 Webview 的代码后，完成必要验证后必须在 `rpg/` 目录运行 `npm run install:local`。
- `npm run install:local` 会重新构建、打包 `dist/python-rpg.vsix` 并用 `code --install-extension ... --force` 替换本机已安装扩展；仅运行 `npm run build` 不足以更新普通 VS Code 窗口。
- 安装完成后，重载 VS Code 窗口并重新打开游戏页面，再报告界面改动可供检查。

## 子代理工具编排

- 命令等待与子代理等待必须严格分流：只有执行命令明确返回 `Script running with cell ID ...` 时，才能把该真实且仍有效的 `cell_id` 交给命令等待工具；等待子代理只能使用协作工具的 `wait_agent`、`list_agents` 等代理接口，严禁用命令等待工具代替。
- 不得用虚构、占位、猜测、复用或已失效的 `cell_id` 调用命令等待工具。当前没有真实 `cell_id` 时，命令等待工具一律不得调用。
- `wait_agent` 超时只表示等待窗口内没有新回执，不表示子代理卡死，也不触发轮询。超时后保持静默等待；不得立即调用 `list_agents`、检查工作区、执行空命令或重复等待。只有用户主动要求状态、子代理主动返回，或已有独立证据表明阻塞时，才重新查询或干预。
- 不得仅凭“一段时间没有新工具调用”中断子代理。先检查协作状态、Token 是否仍增长、是否已有文件/报告产出及任务本身是否处于长推理阶段；只有获得明确阻塞、失败或失活证据后才能中断。中断前向用户说明证据和处理动作。
- 同一工具以相同参数模式返回同一错误一次后，立即停止重试并核对工具签名或切换到正确工具；不得用无意义的状态输出、空命令或重复报错调用尝试“打断”循环。误用等待工具后，不得再调用该错误接口进行自我纠正。
- 子代理派发、完成与失败状态只以协作工具的真实回执为准。`spawn_agent` 未返回成功的 `task_name` 时，不得更新为“已派发”，也不得向用户声称派发成功。
- 显式指定 Worker 类型时，不得使用会继承父代理类型的全历史分叉参数；应使用有限上下文分叉，并在派发后立即核对 Worker 类型和 `task_name` 回执。

## 安全模型：信任本地代码

本项目采用**信任本地代码模型**，不提供对抗恶意代码的隔离边界。玩家在本机运行自己的代码。

### 以下 API 是本地代码执行器的合理使用，审查时不应标记为安全风险

| API | 用途 | 为什么不是风险 |
|---|---|---|
| `exec()` / `compile()` | execute.py 执行玩家 Python 代码 | 玩家主动运行自己的本地代码 |
| `child_process.spawn` | Node 启动本地 Python 子进程 | 只监听 localhost，不暴露公网 |
| `SIGINT` / `SIGKILL` | 中断或终止单次运行子进程 | 管理本地进程生命周期 |
| `__import__` 替换 | 模块白名单 | 防误操作（如 import socket 联网），不防恶意攻击 |
| `shutil.rmtree` | 清理临时工作目录 | 只删 tempfile 创建的目录，不删系统文件 |

### 术语约定

- 用"工作目录隔离"/"进程隔离"描述运行边界，不使用暗示对抗恶意代码的术语
- 统一使用"本地代码执行器"：代码在玩家本机运行，不经过网络

### 头脑风暴前提

讨论本地 Runner 设计前先确认：本项目采用信任本地代码模型，玩家运行自己的代码，不涉及对抗恶意代码。

### 真正的安全问题（需要修复）

- 监听 `0.0.0.0` 或公网地址的网络服务
- 接收外部不可信输入并执行代码
- 将玩家代码发送到远程服务器
- 绕过模块白名单允许 `import socket` / `ssl` / `http` / `subprocess`
