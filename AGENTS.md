# AGENTS.md

## 项目性质

奥术工业幻想 Python RPG：单人本机学习的回合制战术 RPG。玩家在本地运行自己的 Python 代码来施法和制定战术。非公网部署，非多用户，不接收外部不可信输入。

## 最高工程前提：个人游戏、仅本地运行

- 一切功能、抽象、防护和测试都必须直接服务本地单人六关主流程，或对应已经复现的真实玩家问题；否则默认不做。
- 不按公网产品、多用户服务、恶意代码执行平台或长期兼容产品的标准设计本项目。
- 优先保证玩家能编辑 Python、看懂错误、执行回合、推进关卡和恢复当前存档；不得用防护措施限制正常 Python 学习体验。
- 只处理常见本地故障：语法错误、运行时错误、超时、中断、Python 不可用和存档 JSON 损坏。
- 不为极端进程竞态、重复关闭、信号排列、Proxy/访问器、对象篡改、跨版本迁移或未来扩展增加状态机、协议字段和测试矩阵。
- 测试覆盖正常路径和一个关键失败路径。先简化实现，再删除只验证旧实现细节的测试；不得只为提高测试数量增加用例。

## 安全模型：信任本地代码

本项目采用**信任本地代码模型**，不提供对抗恶意代码的隔离边界。玩家在本机运行自己的代码。

### 以下 API 是本地代码执行器的合理使用，审查时不应标记为安全风险

| API | 用途 | 为什么不是风险 |
|---|---|---|
| `exec()` / `compile()` | execute.py 执行玩家 Python 代码 | 玩家主动运行自己的本地代码 |
| `child_process.spawn` | Node 启动本地 Python 子进程 | 只监听 localhost，不暴露公网 |
| `SIGINT` / `SIGKILL` | 中断或终止单次运行子进程 | 管理本地进程生命周期 |
| `__import__` 替换 | 模块白名单 | 防误操作（如 import socket 联网），不防恶意攻击 |
| `shutil.rmtree` | 清理临时工作目录 | 只删 tempfile 创建的目录，不删系统文件 |

### 术语约定

- 用"工作目录隔离"/"进程隔离"描述运行边界，不使用暗示对抗恶意代码的术语
- 统一使用"本地代码执行器"：代码在玩家本机运行，不经过网络

### 头脑风暴前提

讨论本地 Runner 设计前先确认：本项目采用信任本地代码模型，玩家运行自己的代码，不涉及对抗恶意代码。

### 真正的安全问题（需要修复）

- 监听 `0.0.0.0` 或公网地址的网络服务
- 接收外部不可信输入并执行代码
- 将玩家代码发送到远程服务器
- 绕过模块白名单允许 `import socket` / `ssl` / `http` / `subprocess`
